# Problem 2: Currency Swap Form

Small Vite app that fetches Switcheo token prices, lets users choose two currencies, validates the amount, and computes a live swap quote.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
