# Problem 4: Sum to N in TypeScript

This folder contains three unique TypeScript implementations of `sum_to_n`.

File:

- [`sum-to-n.ts`](./sum-to-n.ts)

Notes:

- The prompt uses `func`, but TypeScript uses `function`.
- All implementations support positive, zero, and negative integers.
- The problem statement assumes the result is smaller than `Number.MAX_SAFE_INTEGER`.
